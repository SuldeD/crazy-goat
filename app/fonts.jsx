import { Inter, Rajdhani } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const primary = Rajdhani({
  subsets: ["latin"],
  display: "swap",
  weight: "500",
});
